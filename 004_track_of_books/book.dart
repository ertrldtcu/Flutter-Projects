// import 'package:flutter/material.dart';

class Book {
  final int id;
  final String name, subjects, date;
  bool state = false;
  // late final State numberWidget;
  Book(this.id, this.name, this.subjects, this.date, this.state);
}
